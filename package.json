{
  "name": "weichwarenprojekt",
  "version": "0.1.0",
  "private": true,
  "hostname": "",
  "scripts": {
    "build": "cd client && npm run build",
    "deploy": "node scripts/deploy.js"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "execa": "^5.0.0"
  },
  "engines": {
    "node": ">= 16"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
